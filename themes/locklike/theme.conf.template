# =============================================================================
# CAELESTIA LOCKLIKE — Full Configuration Template
# =============================================================================
# Uncomment and set values to override defaults.
# Locale strings are auto-set by LocaleManager from locales/*.json — leave empty.
# Colors are auto-synced by Caelestia sync.sh — do not touch.
# =============================================================================

[General]

# --- System Info (auto-overridden by sync.sh) ---
os=Arch
host=localhost

# --- Clock ---
# 12-hour mode [true | false] (default: false = 24h)
ap=false

# --- Animations ---
# Crossfade duration in ms (default: 300)
animDuration=300
# Delay before crossfade starts in ms (default: 150)
syncDelay=150
# Background crossfade blur amount 0.0–1.0 (default: 0)
bgBlur=0

# --- Layout ---
# Main card margin from screen edge in px (default: 20)
# mainCardMargin=20
# Spacing between card elements in px (default: 12)
# layoutSpacing=12
# Main card corner radius in px (default: 24)
# mainCardRadius=24
# Avatar circle diameter in px (default: 100)
# avatarCircleSize=100
# Avatar hexagon width in px (default: 100)
# avatarHexagonWidth=100
# Avatar hexagon height in px (default: 120)
# avatarHexagonHeight=120

# --- Avatar ---
# Shape [hexagon | circle] (default: hexagon)
AvatarShape=hexagon

# --- Welcome / Greeting ---
# Enable dynamic greeting message [true | false] (default: true)
enableWelcomeMessage=true

# --- Main Card ---
# Main card component opacity 0.0–1.0 (default: 0.9)
mainCardComponentsOpacity=0.9

# --- Session Picker ---
# Show user session selector [true | false] (default: false)
sessionPicker=false

# --- Power Dialog ---
# Require confirmation for power actions [true | false] (default: true)
powerConfirmEnabled=true
# Power dialog overlay darkness 0.0–1.0 (default: 0.8)
powerOverlayOpacity=0.8

# --- UI Visibility Toggles ---
# Show/hide individual settings panel items [true | false]
# showWelcomeToggle=true
# showWelcomeBgBlur=true
# showAp=true
# showSessionPicker=true
# showPowerConfirm=true
# showAvatarShape=true
# showLanguage=true

# =============================================================================
# LOCALE STRINGS (auto-set by LocaleManager from locales/*.json)
# Leave empty — they are populated at runtime.
# =============================================================================

# --- Greeting ---
greetingMorning=
greetingAfternoon=
greetingNight=

# --- Input ---
passwordPlaceholder=
loadingText=
capsLockWarning=
unlockForNotifications=

# --- Info Fetch ---
fetchTitle=
fetchLabels=

# --- Settings Panel ---
settingsTitle=
animationSpeedLabel=
syncDelayLabel=
backgroundBlurLabel=
language=
close=

# --- Power Dialog ---
shutdown=
reboot=
cancel=
confirmTitle=
confirmShutdown=
confirmReboot=
overlayDarknessLabel=

# --- Welcome Section Labels ---
welcomeMessageLabel=

# --- Card Section Labels ---
cardOpacityLabel=

# --- Toggle Labels ---
apLabel=
sessionPickerLabel=
powerConfirmLabel=

# --- Avatar Shape Dropdown ---
avatarShapeLabel=
hexagonLabel=
circleLabel=

# --- Settings Group Titles ---
groupAnimations=
groupFonts=
groupMainCard=
groupWelcome=
groupPower=
groupSystem=
groupAvatar=
groupLanguage=

# =============================================================================
# COLOR SCHEME (auto-synced by Caelestia sync.sh — DO NOT TOUCH)
# =============================================================================
background=
mainCard=
subComponents=
text=
textDark=
primary=
onPrimary=
secondary=
onSecondary=
tertiary=
onTertiary=
onSuccess=
inverseOnSurface=
surface=
surfaceVariant=
surfaceContainerLow=
surfaceContainer=
surfaceContainerHigh=
outline=
error=
